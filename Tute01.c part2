/* Exercise 1 - Calculations

   Write a C program to input marks of two subjects. Calculate and print the average of the two marks. */

#include <stdio.h>

/* Exercise 1 - Calculations

   Write a C program to input marks of two subjects. Calculate and print the average of the two marks. */

int main() {

  using namespace std;

  float distance;

  cout << "Enter distance the van has travelled :";

  cin >> distance;

  if (distance <= 30) {
    cout << "Amount : " << distance * 50;
  } else {
    cout << "Amount : " << (30 * 50) + (distance - 30) * 40;
  }

   
  
  return 0;
}

   
   
  

